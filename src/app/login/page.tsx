import { LoginContainer } from "@/components/login";

export default function LoginPage() {
    return (
        <div className="w-full h-[80vh] flex justify-center items-center overflow-hidden bg-primary p-10 lg:p-10">
            <LoginContainer/>
        </div>
    )
}